function reverseString(text){
    let result = ""

    for (let i = array.length - 1; i >=0; i--) {
         result += text[i];
         // result = result + text[i]
        
    }

    return result;
}

